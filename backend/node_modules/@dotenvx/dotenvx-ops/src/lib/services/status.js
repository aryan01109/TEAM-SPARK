const Session = require('./../../db/session')

class Status {
  run () {
    const sesh = new Session()
    const status = sesh.status()
    return { status }
  }
}

module.exports = Status
